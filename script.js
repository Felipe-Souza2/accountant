import { isStartInvalid } from "./validation.js"

export function execute() {
  const start = Number(window.document.getElementById('start').value)
  const end = Number(window.document.getElementById('end').value)
  let stepNumber = Number(window.document.getElementById('stepNumber').value)

  
  if (isStartInvalid == '')

}